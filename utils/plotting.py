# utils/plotting.py

"""
These are the plotting utilities for real-time visualization of training. 
We will integrate livelossplot and matplotlib to render loss and accuracy curves 
in the CLI- (terminal + .yaml), TUI- (live config and run control) and notebook-application of the GNN-Playgroumnd.
"""